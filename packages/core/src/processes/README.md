

```yaml

processes:
    MyProcess:
        # Optional
        # if not spcified, then use `/processses/{name}.mjs`
        #classPath: @scope/myPackage/processes/MyProcess.mjs

        ...
        # any params pass to constructor
